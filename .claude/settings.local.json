{
  "projectName": "ai-assist-for-traveller",
  "repoType": "fullstack-app",
  "preferredModel": "claude-sonnet-5",
  "workspace": {
    "root": ".",
    "notes": [
      "PRD.md",
      "README.md",
      "backend/README.md",
      "frontend/README.md",
      "CLAUDE.md"
    ]
  },
  "commands": {
    "backend": "cd backend && python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && uvicorn app.main:app --reload",
    "frontend": "cd frontend && npm install && npm run dev",
    "test": "cd backend && python -m pytest -q",
    "lint": "cd frontend && npm run lint"
  }
}
